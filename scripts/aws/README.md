# scripts/aws/

此目錄的腳本是為舊版自訂 `infra/aws/` IaC 設計的，**已不再使用**。

現已改用官方 LiteLLM Terraform module：
`deploy/aws/production/` → `github.com/BerriAI/litellm//terraform/litellm/aws`

所有 `infra/aws/` 的目錄和檔案已移除。
